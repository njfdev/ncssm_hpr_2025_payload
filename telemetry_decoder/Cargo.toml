[package]
name = "telemetry_decoder"
version = "0.1.0"
edition = "2024"

[dependencies]
payload_types = { path = "../payload_types", features = ["std"] }
serialport = "4"
clap = { version = "4", features = ["derive"] }
